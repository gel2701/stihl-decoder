# Phase44C-R3 reproduction

The authoritative matrix is GitHub Actions run [35524758295](https://github.com/gel2701/stihl-decoder/actions/runs/35524758295), workflow commit `28140424fe1ca1dae98a2ed0085d28702afec737`. Its successful collection job does not mean all historical tests pass. The reviewed classification matrix records 28 equivalent passes and 21 equivalent historical failures in each comparison.

Run `.github/workflows/phase44c-r3-audit.yml` by workflow_dispatch on the audit branch. It checks out full history, pins Node22.23.2, verifies real origin/main, creates exact P/B/C worktrees, runs npm ci separately, and invokes all 49 unchanged test files plus two candidate safety suites. Candidate commit stays `7fde0027ffdf93fbe9276d6b7f831a337bd3dc51`. The harness rejects changes to protected data. Historical audit outputs generated by tests are recorded then restored in disposable worktrees; this never restores or hides protected data writes. Every invocation's stdout, stderr, exit and duration are in the matrix artifact. GitHub also retains compressed portable raw results in job logs and the raw result artifact.

The supplementary read-only masking probe can be reproduced with `R3_AUDIT_ROOT` pointing at a directory containing the exact P/B/C worktrees and `node scripts/phase44c_r3_semantic_probe.mjs`. It does not change tests or suppress assertions. It directly checks the index-query and dossier calculations identified in source review and compares all baseline evidence-validator finding objects.

The existing unmodified CI workflow uses a shallow checkout and fails 27 files on both production and candidate. Six historical-object lookup failures disappear with full history; their exact refs/errors are in `phase44c_r3_ci_confirmation.json`. Missing REFERENCE_COMMIT, pypdf and historical fixture/count/hash debt are preserved and documented, not remediated. No environment failure is represented as a passing test.

Audit acceptance authorizes no main promotion or deployment. Phase44D remains awaiting explicit acceptance.
